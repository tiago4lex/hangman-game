#include <stdio.h>

int main()
{
    char secretword[20];
    sprintf(secretword, "MUSTANG"); // Print String

    int win = 0;
    int hanged = 1;

    do
    {
        /* code */
    } while (!win && !hanged);
}